#ifndef DEDUP_H
#define DEDUP_H

#include <cstdlib>
#include <climits>
#include <cstdio>

/**
 * Deduplicates an array and returns the number of unique elements.
 */
size_t dedup(long arr_size);

#endif // DEDUP_H
